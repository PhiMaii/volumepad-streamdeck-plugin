# Documentation Relocated

This folder is kept only as a compatibility placeholder.

The canonical global documentation is in the repository root at:
`D:\Daten\Programmieren\volumepad\docs`

Source-of-truth policy:
- General architecture and layout docs belong only in the root `docs` folder.
- This local `docs` folder should contain only documentation that is useful and specific to this project.
- AI agents and humans should check root `docs` first for global guidance.
